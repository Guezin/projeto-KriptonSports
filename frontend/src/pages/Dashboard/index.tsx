import React from 'react';

import Layout from '../../components/Layout';

// import { Container, Header, Aside, Nav, Main, Footer } from './styles';

const Dashboard: React.FC = () => {
  return (
    <Layout userName={'Leandro Guezin Jr'}>
      <h1>Dashboard</h1>
    </Layout>
  );
};

export default Dashboard;
